import { CustomMatPaginatorIntl } from './custom-mat-paginator-intl';

describe('CustomMatPaginatorIntl', () => {
  it('should create an instance', () => {
    expect(new CustomMatPaginatorIntl()).toBeTruthy();
  });
});
